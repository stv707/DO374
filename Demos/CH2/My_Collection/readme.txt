ansible-navigator run site.yml --eei $EE -m stdout

## NOTES
#
# Can modify $EE to my execution environment with collections built-in
# or use the HUB EE and have the collections installed.
#
# Need to demonstrate that collections are in the ./collections
# sub-directory.
